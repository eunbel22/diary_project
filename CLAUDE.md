# AI 다이어리 앱 — 프로젝트 개요

## 목적 & 타겟
- **목적**: 부트캠프(AI Product Manager 과정) 포트폴리오용 데모. 실제 배포는 선택사항.
- **타겟 사용자**: ADHD 진단자 및 ADHD 자가의심자
- **핵심 JTBD**: "그냥 말하기만 하면, 판단받지 않고, 며칠 빠져도 부담 없이 다시 돌아올 수 있게 기록을 대신 정리해준다"

## 서비스 컨셉
그날 소비한 것 / 잡힌 약속 / 겪은 일을 **텍스트 또는 음성으로 말하면**, AI가 자동으로 구조화해서 다이어리에 정리해주는 앱. 최초 온보딩 시 대화를 통해 나만의 캐릭터(페르소나)를 생성하고, 이 캐릭터가 이후 모든 상호작용의 인터페이스가 됨.

---

# 핵심 설계 원칙 (ADHD 타겟 반영 — 반드시 지킬 것)

이 원칙들은 As-Is/To-Be 서비스 블루프린트 분석을 거쳐 확정된 것으로, 기능 구현 시 임의로 바꾸지 말 것.

1. **되묻기 최소화**: 필수 정보(소비 항목의 금액 등)만 확인하고, 나머지(장소·시간·감정 등)는 자동 추정 후 `is_estimated` 플래그로 조용히 표시. 강제 확인창 금지.
2. **일정 기록의 날짜는 자동 채움**: 언급 없으면 "오늘"로 저장, 확인 질문 없음.
3. **검토·수정은 선택 사항**: 확인 안 해도 페널티 없이 그대로 저장됨. 완벽주의 압박 유발 금지.
4. **연속 기록(streak) 지표 금지**: all-or-nothing 사고를 유발하는 "N일 연속" 표시 대신 누적형 지표만 사용.
5. **공백 후 복귀에 마찰 없어야 함**: 며칠~몇 주 만에 재접속해도 빠진 기간을 언급하지 않고 자연스럽게 대화 재개. 밀린 일정은 조용히 재정렬 제안(불렛저널의 "이관" 개념 차용).
6. **비판단적(non-judgmental) 톤 고정**: 캐릭터의 시스템 프롬프트에 재촉·훈계·완벽주의 유도 표현 배제 규칙을 반드시 포함.
7. **리마인더는 기본 꺼짐(옵트인)**: 온보딩 마지막에 1회만 부드럽게 제안. 켜진 경우에도 죄책감 유발 문구 금지.
8. **노트·펜의 촉감 대체**: 다이어리 생성 시 캐릭터가 사각사각 연필 소리 + 필사 애니메이션을 재생 — 손글씨의 정서적 가치를 감각적으로 재현.

---

# 기능 흐름 (7단계)

| 단계 | 이름 | 핵심 동작 |
|---|---|---|
| ① | 캐릭터 온보딩 | 대화로 성격·말투·관심사 추출 → 페르소나 생성(이미지 포함, 최초 1회) |
| ② | 일일 기록 입력 | 텍스트/음성으로 소비·일정·사건 말하기 → 필수정보만 확인 |
| ③ | 자동 구조화 | STT 변환 → 개체 추출·분류 |
| — | 다이어리 초안 생성 | 구조화 데이터 + 페르소나 톤 → 문단 생성, 필사 연출 |
| ④ | 검토·수정 | 요청 시에만 캐릭터가 반영해 재작성 |
| ⑤ | 저장·아카이브 | 캘린더 뷰, 누적 마일스톤 → 캐릭터 교체 쿠폰 지급 |
| ⑥ | 공백 후 재참여 | 마지막 접속일 조회 → 자연스러운 재개, 이관 로직 |

---

# 기술 스택

| 레이어 | 도구/기술 | 용도 |
|---|---|---|
| 프론트엔드 | Vite + React + TypeScript | UI 개발 |
| 스타일링 | Tailwind CSS (v4) | UI 스타일링 |
| 백엔드/DB | Supabase (Postgres) | Auth, DB, Storage |
| LLM (텍스트) | Gemini API | 온보딩 대화, NER/분류, 다이어리 생성·재작성, 톤 검증 |
| 음성 입력(STT) | Gemini API (멀티모달 오디오) | 별도 STT API 없이 통합 처리 |
| 이미지 생성 | Gemini API (Imagen) | 캐릭터 이미지 최초 생성 + 쿠폰 재생성 (유료 — 호출 최소화할 것) |
| 배포 | Vercel | 프론트엔드 + 서버리스 함수 호스팅 |
| 서버리스 로직 | Vercel Functions | 자동 구조화 파이프라인 |
| 버전관리 | GitHub (eunbel22/diary_project) | 코드 관리 |

**중요 원칙**:
- Supabase anon key는 공개되어도 안전한 키이므로 `.gitignore`에 넣지 않아도 됨.
- 파생값(마일스톤 카운트, 쿠폰 지급 여부 등)은 클라이언트 값을 신뢰하지 말고 **반드시 DB 트리거로 서버사이드 계산**할 것.
- Vercel과 GitHub 저장소(eunbel22/diary_project)가 연결되어 있어 `main`에 머지되면 자동 배포됨(`vercel --prod` 수동 실행 불필요). 새 Supabase 마이그레이션이 있을 때만 별도로 적용할 것.
- 리마인더·재알림류(저녁 리마인더, 지출 충동 일시정지, 밀린 일정/작업 분해 제안)는 실제 푸시·이메일 발송을 의도적으로 구현하지 않음 — ADHD 타겟에게는 알림 자체가 압박으로 느껴질 수 있어서다. 지출 충동 일시정지·밀린 일정/작업 분해 제안은 서버에 "대기 상태"만 저장해두고 사용자가 앱을 다음에 열었을 때 조용히 배너로만 보여준다(스케줄러 없음). 저녁 리마인더만 예외로 `vercel.json`에 등록된 Vercel Cron(`/api/reminder-candidates`, 매일 실행)이 대상자 목록을 매일 계산해서 로그로 남기지만, 실제 발송 연동은 아직 없음(TODO로 명시돼 있음).

---

# Supabase 스키마

핵심 테이블은 아래와 같음. 로드맵 기능마다 추가된 테이블(`task_status`, `consumption_category`, `consumption_override`, `quick_phrase`, `app_feedback`, `purchase_pause`, `weekly_review`, `task_breakdown`)은 컬럼이 계속 늘어나므로 이 문서에 나열하지 않고, `supabase/migrations/`의 각 파일(파일명이 곧 설명)을 최신 기준으로 삼을 것.

| 테이블 | 주요 컬럼 | 설명 |
|---|---|---|
| `persona` | `user_id`, `name`, `tone`, `image_url`, 기능별 설정 컬럼들, `created_at` | 온보딩 시 1회 생성되는 캐릭터 프로필 + 사용자별 기능 설정(리마인더/인사이트/일시정지/주간회고/작업분해 등 옵트인 토글과 커스텀 옵션) |
| `raw_log` | `id`, `user_id`, `type`(소비/일정/할일/사건), `content`, `is_estimated`, `created_at` | 원본 발화와 추출된 값. **불변** — 수정 대신 `task_status`처럼 별도 테이블로 상태를 관리 |
| `diary_entries` | `id`, `user_id`, `date`, `body`, `version`, `updated_at` | 생성된 다이어리 본문 |
| `coupon` | `user_id`, `entry_count`, `milestone_reached`, `coupons_available`, `used_at`, `updated_at` | 누적 작성 횟수, 캐릭터 교체 쿠폰 상태 (서버 계산 전용 — 클라이언트는 읽기만 가능) |

모든 테이블이 Supabase에 적용되어 있음(`supabase/migrations/0001_...` ~ `0015_task_breakdown.sql`). 새 기능을 추가할 때는 새 마이그레이션 파일을 만들어 여기에 이어 붙일 것.

---

# Gemini API 호출 매핑

| 기능 | 입력 | 출력 | 비고 |
|---|---|---|---|
| 온보딩 대화 | 대화 히스토리(텍스트) | 다음 질문 + 페르소나 특징(JSON) | 비판단적 톤 규칙을 시스템 프롬프트에 고정 |
| STT | 오디오 파일(멀티모달) | 텍스트 전사 결과 | 별도 STT API 없이 Gemini로 처리 |
| NER/분류 | 전사된 텍스트 | 구조화된 JSON(항목·금액·날짜 등) | 필수 필드 누락 여부 함께 판별 |
| 다이어리 생성·재작성 | 구조화 데이터 + 페르소나 톤 | 다이어리 문단(텍스트) | 재작성 시 이전 피드백을 프롬프트에 포함 |
| 이미지 생성(Imagen) | 캐릭터 특징 | 이미지 파일 | 최초 1회 + 쿠폰 사용 시에만 호출 (비용 관리) |

---

# 현재 진행 상황

✅ 완료
- 기본 인프라: Vite + React + TS 프로젝트, Tailwind CSS, Supabase 클라이언트, GitHub 저장소, Vercel 배포(GitHub 연동 자동배포)
- 핵심 7단계 플로우 전체 구현: 캐릭터 온보딩 → 일일 기록 입력(텍스트/음성) → 자동 구조화 → 다이어리 초안 생성(필사 연출 포함) → 검토·수정 → 저장·아카이브(캘린더, 마일스톤/쿠폰) → 공백 후 재참여(밀린 일정 이관)
- ADHD 자가 문항(ASRS 기반), PWA 설치 지원
- 아래 "추가 기능" 로드맵 6개 전부 구현 완료
- 사용자 피드백/별점 기능(설정 탭)
- 치명적 오류 발생 시 `file/incidents.md`에 기록하는 프로세스 정착

기능별 자세한 목록은 `file/list.md`, 치명적 장애 이력은 `file/incidents.md` 참고.

⬜ 다음에 고려할 만한 것
- 홈 화면 바로가기(2번) 라벨 문구 커스터마이징: PWA manifest가 로그인 사용자별로 다르게 나올 수 없어서 일단 대기 항목으로 보류
- `file/list.md`를 새 기능이 추가될 때마다 계속 최신화

---

# 추가 기능 (경쟁앱 리서치 기반) — 로드맵 완료

ADHD 다이어리/생산성 앱 생태계 리서치 결과를 바탕으로 확정했던 기능들. 각 기능은 **"자동으로 해주되, 사용자가 원하는 대로 조정할 수 있게"** — 자동화와 커스터마이징을 함께 제공하는 것이 핵심 원칙이었고, 설정 탭 "나만의 설정" 영역에 커스텀 옵션들을 모아뒀다. 아래 6개 모두 구현·병합 완료(순서: 1 → 2 → 3 → 6 → 4, 5는 아래 사유로 건너뜀).

### 1. 감정↔지출 연결 인사이트 — ✅ 완료
`EmotionSpendingInsight` 컴포넌트. raw_log의 감정 키워드 + 소비 데이터를 조인해 관찰형 카드 생성(판단 없이 관찰만). 켜고 끄기, 관찰 주기(주간/월간), 우선 관찰 감정 선택 가능. 별도 API 호출 없이 조인 쿼리로 구현.

### 2. 홈 화면 빠른 기록 (딥링크) — ✅ 완료 (일부 대기)
PWA manifest shortcuts로 홈 화면에서 바로 입력 모드 진입. 기본 진입 모드(음성/텍스트)는 사용자가 설정 가능. **바로가기 라벨 문구 커스터마이징은 대기 항목** — manifest가 로그인 사용자별로 다르게 나올 수 없는 PWA 구조적 한계로 보류.

### 3. 지출 충동 "일시정지" 옵션 — ✅ 완료
"이거 사고 싶어" 발화 감지 시(옵트인, 기본 꺼짐) 설정한 대기 시간(12h/24h/3일) 뒤 조용히 다시 물어봄. 트리거 기준 금액도 직접 설정. 완료 감지와 같은 패턴으로 서버에서 구매 욕구 여부를 판별.

### 4. 작업 마이크로 분해 (옵트인) — ✅ 완료
크고 막연한 할일을 키워드로 감지해(서버 휴리스틱, 모델 판단 아님) 밀린 일정 배너에 분해 제안을 얹음. 일정 탭에서 항목마다 "쪼개줘"로 개별 요청도 가능. 분해 단계 수(간단히/자세히) 선택 가능.

### 5. 알림 강도 선택지 — ⏭️ 건너뜀
저녁 리마인더는 실제 발송 자체를 의도적으로 구현하지 않은 기능(위 "중요 원칙" 참고)이라, 강도·시간대 옵션을 추가하면 실제로 아무 효과 없는 설정 UI만 늘어나는 셈이라고 판단해 스킵하기로 결정함.

### 6. 주간 회고 리포트 (캐릭터 목소리로) — ✅ 완료
`WeeklyReview` 컴포넌트. 설정한 주기(매주/격주)가 되면 다이어리 탭에서 "돌아보기"를 제안하고, 누르면 그 기간의 다이어리를 캐릭터 목소리로 성취 위주 요약(놓친 것 언급 안 함). 감정 요약 포함 여부 선택 가능. 오늘의 다이어리 생성과 같이 사용자가 직접 눌렀을 때만 Gemini를 호출.

---

# 참고 링크
- GitHub: https://github.com/eunbel22/diary_project
- Vercel(프로덕션): https://diary-app-snowy-psi.vercel.app
- Vercel 대시보드: https://vercel.com/eunbel22s-projects/diary-app
- 로컬 경로: `C:\Users\qwr01\dev\diary-app`