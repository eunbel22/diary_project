# AI 다이어리 앱 — 프로젝트 개요

## 목적 & 타겟
- **목적**: 부트캠프(AI Product Manager 과정) 포트폴리오용 데모. 실제 배포는 선택사항.
- **타겟 사용자**: ADHD 진단자 및 ADHD 자가의심자
- **핵심 JTBD**: "그냥 말하기만 하면, 판단받지 않고, 며칠 빠져도 부담 없이 다시 돌아올 수 있게 기록을 대신 정리해준다"

## 서비스 컨셉
그날 소비한 것 / 잡힌 약속 / 겪은 일을 **텍스트 또는 음성으로 말하면**, AI가 자동으로 구조화해서 다이어리에 정리해주는 앱. 최초 온보딩 시 대화를 통해 나만의 캐릭터(페르소나)를 생성하고, 이 캐릭터가 이후 모든 상호작용의 인터페이스가 됨.

---

# 핵심 설계 원칙 (ADHD 타겟 반영 — 반드시 지킬 것)

이 원칙들은 As-Is/To-Be 서비스 블루프린트 분석을 거쳐 확정된 것으로, 기능 구현 시 임의로 바꾸지 말 것.

1. **되묻기 최소화**: 필수 정보(소비 항목의 금액 등)만 확인하고, 나머지(장소·시간·감정 등)는 자동 추정 후 `is_estimated` 플래그로 조용히 표시. 강제 확인창 금지.
2. **일정 기록의 날짜는 자동 채움**: 언급 없으면 "오늘"로 저장, 확인 질문 없음.
3. **검토·수정은 선택 사항**: 확인 안 해도 페널티 없이 그대로 저장됨. 완벽주의 압박 유발 금지.
4. **연속 기록(streak) 지표 금지**: all-or-nothing 사고를 유발하는 "N일 연속" 표시 대신 누적형 지표만 사용.
5. **공백 후 복귀에 마찰 없어야 함**: 며칠~몇 주 만에 재접속해도 빠진 기간을 언급하지 않고 자연스럽게 대화 재개. 밀린 일정은 조용히 재정렬 제안(불렛저널의 "이관" 개념 차용).
6. **비판단적(non-judgmental) 톤 고정**: 캐릭터의 시스템 프롬프트에 재촉·훈계·완벽주의 유도 표현 배제 규칙을 반드시 포함.
7. **리마인더는 기본 꺼짐(옵트인)**: 온보딩 마지막에 1회만 부드럽게 제안. 켜진 경우에도 죄책감 유발 문구 금지.
8. **노트·펜의 촉감 대체**: 다이어리 생성 시 캐릭터가 사각사각 연필 소리 + 필사 애니메이션을 재생 — 손글씨의 정서적 가치를 감각적으로 재현.

---

# 기능 흐름 (7단계)

| 단계 | 이름 | 핵심 동작 |
|---|---|---|
| ① | 캐릭터 온보딩 | 대화로 성격·말투·관심사 추출 → 페르소나 생성(이미지 포함, 최초 1회) |
| ② | 일일 기록 입력 | 텍스트/음성으로 소비·일정·사건 말하기 → 필수정보만 확인 |
| ③ | 자동 구조화 | STT 변환 → 개체 추출·분류 |
| — | 다이어리 초안 생성 | 구조화 데이터 + 페르소나 톤 → 문단 생성, 필사 연출 |
| ④ | 검토·수정 | 요청 시에만 캐릭터가 반영해 재작성 |
| ⑤ | 저장·아카이브 | 캘린더 뷰, 누적 마일스톤 → 캐릭터 교체 쿠폰 지급 |
| ⑥ | 공백 후 재참여 | 마지막 접속일 조회 → 자연스러운 재개, 이관 로직 |

---

# 기술 스택

| 레이어 | 도구/기술 | 용도 |
|---|---|---|
| 프론트엔드 | Vite + React + TypeScript | UI 개발 |
| 스타일링 | Tailwind CSS (v4) | UI 스타일링 |
| 백엔드/DB | Supabase (Postgres) | Auth, DB, Storage |
| LLM (텍스트) | Gemini API | 온보딩 대화, NER/분류, 다이어리 생성·재작성, 톤 검증 |
| 음성 입력(STT) | Gemini API (멀티모달 오디오) | 별도 STT API 없이 통합 처리 |
| 이미지 생성 | Gemini API (Imagen) | 캐릭터 이미지 최초 생성 + 쿠폰 재생성 (유료 — 호출 최소화할 것) |
| 배포 | Vercel | 프론트엔드 + 서버리스 함수 호스팅 |
| 서버리스 로직 | Vercel Functions | 자동 구조화 파이프라인 |
| 알림 스케줄링 | Vercel Cron + Supabase Edge Function | 옵트인 리마인더 트리거 |
| 버전관리 | GitHub (eunbel22/diary_project) | 코드 관리 |

**중요 원칙**:
- Supabase anon key는 공개되어도 안전한 키이므로 `.gitignore`에 넣지 않아도 됨.
- 파생값(마일스톤 카운트, 쿠폰 지급 여부 등)은 클라이언트 값을 신뢰하지 말고 **반드시 DB 트리거로 서버사이드 계산**할 것.
- `vercel --prod`로 수동 배포 방식 사용 중 (Git 자동배포 연결 안 함).

---

# Supabase 스키마

| 테이블 | 주요 컬럼 | 설명 |
|---|---|---|
| `persona` | `user_id`, `name`, `tone`, `image_url`, `created_at` | 온보딩 시 1회 생성되는 캐릭터 프로필 |
| `raw_log` | `id`, `user_id`, `type`(소비/일정/사건), `content`, `is_estimated`, `created_at` | 원본 발화와 추출된 값 |
| `diary_entries` | `id`, `user_id`, `date`, `body`, `version`, `updated_at` | 생성된 다이어리 본문 |
| `coupon` | `user_id`, `entry_count`, `milestone_reached`, `used_at` | 누적 작성 횟수, 캐릭터 교체 쿠폰 상태 (서버 계산 전용) |

⚠️ **아직 실제로 테이블이 생성되지 않은 상태** — 이 스키마를 기준으로 SQL을 작성해서 Supabase에 적용해야 함.

---

# Gemini API 호출 매핑

| 기능 | 입력 | 출력 | 비고 |
|---|---|---|---|
| 온보딩 대화 | 대화 히스토리(텍스트) | 다음 질문 + 페르소나 특징(JSON) | 비판단적 톤 규칙을 시스템 프롬프트에 고정 |
| STT | 오디오 파일(멀티모달) | 텍스트 전사 결과 | 별도 STT API 없이 Gemini로 처리 |
| NER/분류 | 전사된 텍스트 | 구조화된 JSON(항목·금액·날짜 등) | 필수 필드 누락 여부 함께 판별 |
| 다이어리 생성·재작성 | 구조화 데이터 + 페르소나 톤 | 다이어리 문단(텍스트) | 재작성 시 이전 피드백을 프롬프트에 포함 |
| 이미지 생성(Imagen) | 캐릭터 특징 | 이미지 파일 | 최초 1회 + 쿠폰 사용 시에만 호출 (비용 관리) |

---

# 현재 진행 상황

✅ 완료
- Vite + React + TS 프로젝트 생성 (`diary-app` 폴더, `C:\Users\qwr01\dev\diary-app`)
- Tailwind CSS 설정 및 적용 확인
- Supabase 클라이언트 연결 (`src/supabaseClient.ts`)
- GitHub 리포지토리 연결 및 첫 커밋 푸시 (https://github.com/eunbel22/diary_project)
- Vercel 프로덕션 배포 (https://diary-app-snowy-psi.vercel.app)
- Vercel 환경변수(`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) 등록

⬜ 미완료 (다음 작업)
- Supabase에 위 4개 테이블 실제 생성 (SQL 스키마 적용)
- 온보딩 대화 컴포넌트 구현 (캐릭터 생성 플로우)
- 일일 기록 입력 UI (텍스트/음성 입력창)
- Gemini API 연동 (온보딩 대화 → NER → 다이어리 생성 파이프라인)
- 다이어리 카드 UI + 필사 연출(사각사각 소리, 애니메이션)
- 캘린더 뷰, 마일스톤/쿠폰 로직
- 리마인더 옵트인 플로우 (Vercel Cron + Edge Function)

---

# 추가 기능 (경쟁앱 리서치 기반, 우선순위 확정)

ADHD 다이어리/생산성 앱 생태계 리서치 결과를 바탕으로 확정한 다음 기능들. 각 기능은 **"자동으로 해주되, 사용자가 원하는 대로 조정할 수 있게"** — 자동화와 커스터마이징을 함께 제공하는 것이 핵심 원칙. 설정 탭에 "나만의 설정" 영역을 신설해서 아래 커스텀 옵션들을 모아둘 것.

## 🟢 즉시 착수

### 1. 감정↔지출 연결 인사이트
- **기본 동작**: raw_log의 감정 키워드 + 소비 데이터를 조인해서 "이번 주 '스트레스' 감정이 있던 날 소비가 평소보다 많았어요" 같은 관찰형 카드 생성. 판단 없이 관찰만.
- **커스텀 옵션**:
  - 이 기능 자체를 켜고 끄기 (기본 켜짐)
  - 관찰 주기 선택: 주간 / 월간
  - 어떤 감정 키워드를 우선적으로 관찰할지 사용자가 선택(자주 쓰는 감정 태그 중에서)
- 별도 API 호출 없이 기존 데이터 조인 쿼리로 구현.

### 2. 홈 화면 빠른 기록 (딥링크)
- **기본 동작**: 홈 화면 아이콘에서 바로 입력 모드로 진입해 캡처 마찰 최소화.
- **커스텀 옵션**:
  - 기본 진입 모드를 음성/텍스트 중 사용자가 선택
  - 바로가기 라벨 문구를 직접 수정 가능(PWA manifest shortcuts 활용)
- PWA 특성상 완전한 네이티브 위젯은 어려우니 딥링크 수준으로 구현.

### 3. 지출 충동 "일시정지" 옵션 (옵트인)
- **기본 동작**: "이거 사고 싶어" 같은 발화 감지 시, 캐릭터가 "내일 다시 물어볼까?" 하고 하루 뒤 부드럽게 리마인드.
- **커스텀 옵션**:
  - 기능 자체는 기본 꺼짐(옵트인)
  - 대기 시간 선택: 12시간 / 24시간 / 3일
  - 트리거 기준 금액 사용자가 직접 설정(예: 3만원 이상만 반응)
- 기존 "완료 감지" 로직과 유사한 패턴(문장 유사도 매칭)으로 구현.

## 🟡 중기 검토

### 4. 작업 마이크로 분해 (옵트인)
- **기본 동작**: 크고 막연한 할일 감지 시 캐릭터가 "잘게 나눠줄까?" 제안. 밀린 일정 이관 배너에 얹어서 노출.
- **커스텀 옵션**:
  - 옵트인 토글(기본 꺼짐), 항목별로 개별 요청도 가능("쪼개줘" 버튼)
  - 분해 단계 수 선택: 간단히(2~3단계) / 자세히(4~5단계)

### 5. 알림 강도 선택지
- **기본 동작**: 기존 저녁 리마인더(기본 꺼짐, 옵트인)에 강도 옵션 추가.
- **커스텀 옵션**:
  - 강도: 조용(soft) / 보통(normal) 중 선택 — 기본값은 조용
  - 리마인더 시간대 직접 설정(기본 저녁 시간대에서 자유롭게 변경 가능)

### 6. 주간 회고 리포트 (캐릭터 목소리로)
- **기본 동작**: "이번 주 이런 순간들이 있었어" — 성취 위주 요약, 놓친 것은 언급하지 않음. Gemini로 주 1회 생성해 다이어리 아카이브에 카드로 추가.
- **커스텀 옵션**:
  - 주기 선택: 매주 / 격주
  - 포함 항목 선택: 성취만 / 성취+감정 요약 함께

## 구현 순서 제안
1번(감정-지출) → 5번(알림 강도, 기존 리마인더 확장이라 공수 적음) → 2번(딥링크) → 3번(일시정지) → 6번(주간 리포트) → 4번(작업 분해, 가장 복잡)

---

# 참고 링크
- GitHub: https://github.com/eunbel22/diary_project
- Vercel(프로덕션): https://diary-app-snowy-psi.vercel.app
- Vercel 대시보드: https://vercel.com/eunbel22s-projects/diary-app
- 로컬 경로: `C:\Users\qwr01\dev\diary-app`